# Task references non-existent dependency
my_dag:
  default_args:
    owner: airflow
    start_date: "2024-01-01"
  schedule: "@daily"
  tasks:
    task_1:
      operator: BashOperator
      dependencies:
        - nonexistent_task
